let n = parseInt(prompt('Inserisci un numero'));

for (let i=0; i < n; i++) {

    if ()
            
    if (isNaN(n)) {
        alert('il valore inserito non è numerico');
        console.log('non è un numero');
    }
}

function cubo(n) {
    var operazione = n * n * n;
    return operazione;
    }

